package enums;

public enum PrintBoardType {
        DEFAULT,
        NUMBERS,
        LEGAL_MOVES,
        UNDER_PRESSURE,
        PROTECTED,
        EN_PASSANT,
        CHECK_LINES,
        PINNED_AND_PINNED_CHECK_LINES
}
